# Publish only the md5/sha1 checksums Maven Central requires. sha256/sha512 are
# accepted but optional, and every extra file counts against the Central Portal
# monthly file-count limit. See PUBLISHING.md.
systemProp.org.gradle.internal.publish.checksums.insecure=true
