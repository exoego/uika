> prepareVendoredJar
> app/compile
> checkDump

# The shell invocation must run the merge once for the whole build.
> uikaDumpClasspath
> checkDumpRanOnce

# No uikaJfr anywhere in this build, so forked test JVMs get no recording flag.
> checkTestJavaOptionsUntouched
