# The gate firing IS the plugin's purpose, so the build must FAIL here. Without
# invoker.buildResult the invoker treats a failed build as a failed test, and the
# whole exit-1 path would stay untestable.
invoker.buildResult=failure
# -U: the shared it-repo caches resolution failures across invoker runs.
invoker.goals=uika:upgrade-check -U -Duika.cliVersion=9.9.9 -Duika.before=before.json -Duika.after=after.json
