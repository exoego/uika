"""The uika release this ruleset belongs to.

Placeholder in git, stamped by the release packaging step. It is what the uika-cli
download defaults to, so the ruleset and the binary it runs always match without a
second version to bump.
"""

UIKA_VERSION = "0.0.0-dev"
