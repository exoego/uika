# Notes for Agents: cli/

The check pipeline and linkage notes moved to `../cli-java/AGENTS.md`, next to the
implementation they describe. The semantics there hold for this crate too, which
produced them.
